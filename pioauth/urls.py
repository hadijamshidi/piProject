from django.conf.urls import url
from pioauth import views

urlpatterns = [
    # url('', views.Home.as_view()),
    # url('offline.html', views.Offline.as_view()),
    # url('poly2', views.Poly2View.as_view()),
]
